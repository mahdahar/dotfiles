---
description: Stage (git add .), create a detailed Conventional Commit, then push.
argument-hint: "[extra commit context]"
---
Use the **git-ship** skill now.

Requirements:
- Run `git add .`
- Create a detailed **Conventional Commit** message from staged changes.
- Commit and then run `git push`.
- If provided, include this extra context in the commit body: $@
